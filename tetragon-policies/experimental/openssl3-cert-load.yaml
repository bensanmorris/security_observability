apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: openssl3-cert-load
spec:
  uprobes:
  # SSL_CTX_use_certificate_file(SSL_CTX*, const char* file, int type)
  # SSL_CTX_use_certificate_chain_file(SSL_CTX*, const char* file)
  # arg[1] is the certificate file path — captured as a string.
  # RHEL8/9 path — Tetragon v1.1.0 compatible
  - path: "/usr/lib64/libssl.so.3"
    symbols:
    - "SSL_CTX_use_certificate_file"
    - "SSL_CTX_use_certificate_chain_file"
    args:
    - index: 0
      type: "uint64"
    - index: 1
      type: "string"

  # d2i_X509(X509 **px, const unsigned char **in, long len)
  # arg[1] is a pointer-to-pointer to the DER buffer; arg[2] is its length.
  # Note: 'char_buf' used instead of 'bytes' for Tetragon v1.1.0 compatibility.
  # Note: 'selectors' removed — only matchPIDs supported in v1.1.0 uprobes.
  - path: "/usr/lib64/libssl.so.3"
    symbols:
    - "d2i_X509"
    args:
    - index: 0
      type: "uint64"
    - index: 1
      type: "char_buf"
      sizeArgIndex: 3
    - index: 2
      type: "size_t"
