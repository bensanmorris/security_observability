psutil>=5.9.0
grpcio==1.60.1
grpcio-tools==1.60.1
protobuf==4.25.3
prometheus-client==0.19.0
cryptography==41.0.7      # also provides PKCS12 (.p12/.pfx) support
pyyaml==6.0.1
kubernetes>=28.1.0
pyjks==20.0.0             # JKS (.jks/.keystore/.truststore) support
pyasn1==0.6.3             # raw SPKI algorithm OID decode (fips_compliance_checker.get_algorithm_oids);
pyasn1-modules==0.4.2     # already a transitive dep of pyjks, pinned explicitly since we import it directly

# Optional: Kafka event publishing
# Enable by setting [kafka] enabled = true in cert-analyzer.conf or KAFKA_ENABLED=true.
# The analyzer degrades gracefully to Prometheus-only if this package is absent.
kafka-python==2.0.3
