psutil>=5.9.0
grpcio==1.68.1
grpcio-tools==1.68.1      # protobuf<6.0dev,>=5.26.1 -- must track protobuf's major version
protobuf==5.29.6
prometheus-client==0.19.0
cryptography==50.0.0      # also provides PKCS12 (.p12/.pfx) support
pyyaml==6.0.1
kubernetes>=28.1.0
pyjks==20.0.0             # JKS (.jks/.keystore/.truststore) support

# Optional: Kafka event publishing
# Enable by setting [kafka] enabled = true in cert-analyzer.conf or KAFKA_ENABLED=true.
# The analyzer degrades gracefully to Prometheus-only if this package is absent.
kafka-python==2.3.2
